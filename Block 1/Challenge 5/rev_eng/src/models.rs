use crate::utils;


// This replaces the C++ Book class.
pub struct Book{
    pub isbn: String,
    pub title: String,
    pub author: String,
    pub pages: Vec<String>,
}

pub struct User{
    // Nothing Here Yet.
}

pub struct BookReadingSession{
    // Nothing Here Yet.
}